{
  "name": "selfsigned-ca",
  "version": "1.0.0",
  "description": "🔑 Generate self-signed certificates and keys and CA certs for use in HTTPS servers.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mike Kovařík",
  "license": "MIT",
  "devDependencies": {
    "rollup": "^0.64.1",
    "rollup-plugin-notify": "^1.0.3"
  },
  "dependencies": {
    "node-forge": "^0.7.5"
  }
}
